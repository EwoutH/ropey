image: Visual Studio 2017

environment:
  host: x86_64-pc-windows-msvc
  matrix:
    - platform: x86_64
      target: x86_64-pc-windows-msvc
      channel: stable
    - platform: arm64
      target: aarch64-pc-windows-msvc
      channel: stable
matrix:
  allow_failures:
    - platform: arm64

install:
    - git submodule update --init
    - appveyor DownloadFile https://win.rustup.rs/ -FileName rustup-init.exe  # Downloads Rustup-init 
    - rustup-init -yv --default-toolchain %channel% --default-host %host%     # Installs Rust
    - set PATH=%PATH%;%USERPROFILE%\.cargo\bin;%APPVEYOR_BUILD_FOLDER%        # Adds Cargo to path
    - rustc -vV
    - cargo -vV
    - rustup target add %target%

build_script:
    - cargo build --release --target=%target%

test_script:
    - cargo test --target=%target% --verbose

artifacts:
    - path: target\$(target)\release\*.*
      name: $(APPVEYOR_PROJECT_NAME)-$(platform)
      
deploy:
  - provider: GitHub
    artifact: $(APPVEYOR_PROJECT_NAME)-$(platform)
    auth_token:
      secure: 'hY5Mk6KOwgQ97TzEBsM7Woqr1ZIm5QTvHg8EvxMV1x8j3wk/3mNBMqWFFbEIBK0i'
    prerelease: true
    on:
      appveyor_repo_tag: true
